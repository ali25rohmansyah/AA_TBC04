<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gregetdev.materitbcapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PostTest"></activity>
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HalamanMateri" />
        <activity android:name=".MateriVideoActivity" />
        <activity android:name=".WebMainActivity" />
        <activity android:name=".UserDetail" />
        <activity android:name=".InputBiodata" />
        <activity android:name=".Pretest" />
        <activity android:name=".TanggalDiagnosa" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".HalamanUser" />
        <activity android:name=".Alarm" /> <!-- Register the Alarm Receiver -->
        <receiver
            android:name=".AlarmSet.AlarmReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>